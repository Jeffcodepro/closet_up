<!DOCTYPE html>
<html>
  <head>
    <title>ClosetUp</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
  </head>

  <body>
    <%= render "shared/flashes" %>
    <%= yield %>

      <footer class="footer bg-dark text-white py-4 mt-5">
        <div class="container">
          <div class="row">
            <div class="col-md-4">
              <h5>Navegação</h5>
              <ul class="list-unstyled">
                <li><%= link_to "Início", root_path, class: "footer-list" %></li>
                <li><%= link_to "Produtos", products_path, class: "footer-list" %></li>
                <li><%= link_to "Minha Conta", edit_user_registration_path, class: "footer-list" %></li>
              </ul>
            </div>

            <div class="col-md-4">
              <h5>Contato</h5>
              <ul class="list-unstyled">
                <li>Email: contato@marketplace.com</li>
                <li>Telefone: (00) 1234-5678</li>
              </ul>
            </div>

            <div class="col-md-4">
              <h5>Siga-nos</h5>
              <ul class="list-inline">
                <li class="list-inline-item">
                  <a href="#" class="footer-icons">
                    <i class="bi bi-facebook" style="font-size: 1rem;"></i>
                  </a>
                </li>
                <li class="list-inline-item">
                  <a href="#" class="footer-icons">
                    <i class="bi bi-instagram" style="font-size: 1rem;"></i>
                  </a>
                </li>
                <li class="list-inline-item">
                  <a href="#" class="footer-icons">
                    <i class="bi bi-twitter" style="font-size: 1rem;"></i>
                  </a>
                </li>
              </ul>
            </div>
          </div>
          <hr class="bg-secondary">
          <div class="text-center text-secondary footer-copy">
            <p class="footer-copy">&copy; <%= Time.now.year %> ClosetUp. Todos os direitos reservados.</p>
          </div>
        </div>
    </footer>
    <script>
      // Aguarda 3 segundos (3000 milissegundos) antes de começar a esconder o aviso
      setTimeout(function() {
        // Encontra todos os elementos com a classe 'alert'
        const alerts = document.querySelectorAll('.alert');

        // Itera sobre cada alerta para fazê-lo desaparecer
        alerts.forEach(function(alert) {
          // Inicia a transição de opacidade para um fade-out suave
          alert.style.transition = 'opacity 0.5s ease-out';
          alert.style.opacity = '0';

          // Após a transição, remove o elemento do DOM para liberar o espaço
          setTimeout(function() {
            alert.remove();
          }, 500); // Aguarda 0.5 segundos (500ms) para remover, igual ao tempo da transição
        });
      }, 3000);
    </script>

  </body>
</html>
