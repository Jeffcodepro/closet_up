<div class="fixed-top">
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
      <a class="navbar-brand logo" href="/">
          <%= image_tag "closetup_logo.png", alt: "ClosetUp Logo", height: 90 %>
      </a>

      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarSupportedContent">

    <ul class="navbar-nav d-flex flex-row gap-3 ms-auto">
      <% if user_signed_in? %>
        <li class="nav-item">
          <%= link_to orders_path, class: "nav-link text-white" do %>
            Olá, <%= current_user.email %>
          <% end %>
        </li>
        <li class="nav-item">
          <%= link_to "Adicionar Produto", new_product_path, class: "nav-link btn-add-product" %>
        </li>
        <li class="nav-item">
          <%= link_to edit_user_registration_path, class: "nav-link text-white" do %>
          <i class="bi bi-person fs-4"></i>
          <% end %>
        </ul>
      </div>
    </div>
  </nav>
</div>

<nav class="navbar navbar-dark bg-secondary">
  <div class="container-fluid">
    <ul class="navbar-nav d-flex flex-row w-100 justify-content-between overflow-auto">
      <% Category.all.each do |category| %>
        <li class="container nav-item text-center">
          <%= link_to products_by_category_path(category), class: "nav-link text-white px-2" do %>
            <%= category.title %>
          <% end %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>


<div class="container py-4">
  <div class="row">
    <div class="col">
      <h1>Meus Pedidos</h1>

      <% if @orders.empty? %>
        <p>Você ainda não fez nenhum pedido.</p>
        <%= link_to 'Compre Agora', products_path, class: 'btn btn-primary' %>
      <% else %>
        <table class="table table-hover">
          <thead>
            <tr>
              <th>ID do Pedido</th>
              <th>Produto</th>
              <th>Tamanho</th>
              <th>Valor</th>
              <th>Status</th>
              <th>Data do Pedido</th>
              <th></th>
            </tr>
          </thead>
          <tbody>
            <% @orders.each do |order| %>
              <% items = order.order_items %>
              <% first = items.first %>
              <% extra = [items.size - 1, 0].max %>

              <tr>
                <td>#<%= order.id %></td>

                <td>
                  <%= first&.product&.title || "—" %>
                  <% if extra > 0 %>
                    <small class="text-muted">+ <%= extra %> outros</small>
                  <% end %>
                </td>

                <td>
                  <%= items.map(&:size).compact_blank.uniq.join(", ").presence || "-" %>
                </td>

                <td>
                  <%= number_to_currency(order.amount_cents / 100.0, unit: "R$ ", separator: ",", delimiter: ".") %>
                </td>

                <td><%= order.state %></td>
                <td><%= l(order.created_at.to_date) %></td>
                <td><%= link_to 'Ver Detalhes', order_path(order), class: 'btn-order-info' %></td>
              </tr>
            <% end %>
          </tbody>

        </table>
      <% end %>

      <%= link_to 'Retornar para produtos', products_path, class: 'btn btn-secondary mt-3' %>
    </div>
  </div>
</div>
